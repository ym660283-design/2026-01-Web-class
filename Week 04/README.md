# 2026-03-26

## 오늘 학습한 내용

1. `java_example.html`
   `var`, `let`, `const`를 사용해 이름, 나이, 학번을 저장하는 방법을 연습했습니다.

2. `java_example.html`
   전역변수와 지역변수의 차이를 함수로 구분하고, `alert()`로 결과를 출력하는 방법을 학습했습니다.

3. `ifwhile.html`
   `for`문을 사용해 1부터 10까지 반복하고, `if`문으로 짝수와 홀수를 판별해 화면에 출력했습니다.

4. `function.html`
   함수를 이용해 두 수의 합계를 계산하고, 그 결과를 표 형태로 출력하는 방법을 학습했습니다.

5. `array_basic.html`
   10개의 정수 배열을 만들고 무작위 값을 저장한 뒤, 평균을 계산하고 `filter()`로 60점 미만 점수를 추출했습니다.

6. `student.html`
   이름, 학번, 전공을 속성으로 가지는 객체를 만들고, 자기소개 문장을 반환하는 메서드를 작성했습니다.

7. `change_image.html`
   이미지의 `src`를 직접 비교하지 않고 `count` 값을 이용해 홀수와 짝수에 따라 다른 이미지를 출력하는 방법을 학습했습니다.

## 추가학습

ToDo 리스트 앱을 만들 때 AI에게 아래와 같이 요청하면 기획, 구현, 개선을 더 쉽게 진행할 수 있습니다.

1. 기능 설계 프롬프트
   `HTML, CSS, JavaScript를 사용해서 ToDo 리스트 앱에 필요한 기능을 정리해줘. 할 일 추가, 완료 처리, 삭제, 전체 개수 표시 기능을 포함해줘.`

2. 기본 코드 생성 프롬프트
   `초보자도 이해할 수 있게 HTML, CSS, JavaScript로 간단한 ToDo 리스트 앱 예제를 만들어줘. 각 코드에 설명 주석도 함께 추가해줘.`

3. 이벤트 처리 학습 프롬프트
   `ToDo 리스트 앱에서 버튼 클릭으로 할 일을 추가하고, 체크박스를 클릭하면 완료 표시가 되도록 JavaScript 코드를 작성해줘.`

4. 디자인 개선 프롬프트
   `ToDo 리스트 앱 UI를 더 깔끔하게 보이도록 CSS를 개선해줘. 카드형 레이아웃과 hover 효과를 넣어줘.`

5. 확장 기능 프롬프트
   `ToDo 리스트 앱에 검색 기능과 완료된 항목만 보기 기능을 추가하는 방법을 단계별로 설명해줘.`
